# Frontend CantusDei

Panel React/Vite inicial para consumir la API fullstack.

## Comandos

```bash
npm install
npm run dev
```

Si el backend no corre en `http://localhost:4100`, crear un archivo `.env`:

```text
VITE_API_URL=http://localhost:4100/api
```
